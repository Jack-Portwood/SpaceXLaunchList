import React,{Fragment} from 'react'

const Options = ({item}) =>{ 

      return (
        <Fragment>
          <option>{item}</option>
        </Fragment>
      );
    };



 export default Options;
