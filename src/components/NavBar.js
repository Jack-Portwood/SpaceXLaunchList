import React from 'react';
import image1 from "../assets/spacex-logo";

const NavBar = ()=>{ 

    return(
        <header className="navBar" bgImage={image1}> 
        <button>CLick</button>
        </header>
    )

}

export default NavBar;